# This is the main behat.yml file

default:
    suites:
        default:
          contexts: [ "LinkORB\TransmogrifierExtension\TransmogrifierContext" ]
    extensions:
        LinkORB\TransmogrifierExtension\Extension:
            dbconf_dir: /share/config/database/
            dataset_dir: ../transmogrifier/example/
